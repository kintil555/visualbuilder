# Base URL of the deployed apps/api Worker. Used for all auth + API calls.
# Dev:        http://localhost:8787
# Production: https://visual-builder-api.<your-subdomain>.workers.dev
NEXT_PUBLIC_API_URL="http://localhost:8787"
